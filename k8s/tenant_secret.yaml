apiVersion: v1
kind: Secret
metadata:
  name: tenant-secret-{{TENANT_ID}}         # e.g. tenant-secret-abc123
type: Opaque
stringData:
  token: "{{TENANT_TOKEN}}"                 # opaque 256-bit string